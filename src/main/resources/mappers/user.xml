<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.triple.mapper.UserMapper">
	
	
	<!-- 전체 사용자 조회 -->
	<select id="getAllUserInfo" resultType="userDto">
		select userId, nickname, password, registrationDate, nicknameModifiedDate, isUnsigned, unsignedDate, isRecommendated  
		from user
		order by registrationDate
	</select>

	<!-- 사용자 상세 조회 -->
<!-- 	<select id="userInfo" parameterType="string" resultType="userDto">
		select *
		from user 
		where userId = UNHEX(REPLACE(#{articleno}, '-', ''))
	</select> -->
	
	<!-- 사용자 조회 -->
	<select id="getUserInfo" parameterType="string" resultType="userDto">
		select userId, nickname, password, registrationDate, nicknameModifiedDate, isUnsigned, unsignedDate, isRecommendated 
		from user 
		where userId = #{userid}
	</select>
	
	
</mapper>