<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.triple.mapper.AttachedPhotoMapper">
	
	
	<!-- 전체 사진 정보 조회 -->
	<select id="getAllPhotoInfo" resultType="attachedPhotoDto">
		select attachedPhotoId, attachedActivityType, attachedActivityId, uploadUserId, uploadedDate, isDeleted, deletedDate  
		from attachedphoto
		order by uploadedDate desc
	</select>

	
	<!-- 특정 사진 정보 조회 -->
	<select id="getPhotoInfo" parameterType="String" resultType="attachedPhotoDto">
		select attachedPhotoId, attachedActivityType, attachedActivityId, uploadUserId, uploadedDate, isDeleted, deletedDate  
		from attachedphoto
		where attachedPhotoId = #{photoid}
	</select>
	
	<!-- 특정 활동 관련 사진 정보 조회 -->
	<select id="getSpecificAcitivityPhotoInfo" resultType="attachedPhotoDto">
		select attachedPhotoId, attachedActivityType, attachedActivityId, uploadUserId, uploadedDate, isDeleted, deletedDate  
		from attachedphoto
		where attachedActivityId = #{activityId}
	</select>
	
</mapper>